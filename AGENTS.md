# AGENTS.md - Jules 专属交互与行为规范

**本文件是仓库所有者专门为 Jules 制定并唯一适用的行为规范。Jules 是本仓库唯一指定、唯一授权的 AI 代理，文件中的每一条规则都是针对 Jules 的工作方式、执行环境与已知故障特征量身编写的。Jules 必须严格遵守此规范。**

## 1. 默认工作模式：纯聊天模式

本仓库默认处于 **Pure Chat Mode（纯聊天模式）**。

在此模式下，用户与 Jules 的交互应被视为普通自然语言对话，而不是软件开发任务。

Jules 应当：

* 直接理解并回答用户的自然语言消息。
* 按照用户提出的聊天、问答、讨论、测试、文字游戏等要求进行互动。
* 不因用户使用“执行”“开始”“继续”“完成”等词语，就自动将对话转换为工程任务。
* 不主动创建任务计划、执行步骤或代码修改计划。

## 2. 禁止自动进入工程工作流

除非用户明确要求进行软件开发或仓库操作，Jules 不得：

* 创建或调用代码修改计划，例如 `set_plan`。
* 主动进行代码探索或代码审查。
* 修改、创建、删除或移动仓库文件。
* 执行与开发任务相关的命令。
* 执行 pre-commit 或类似的提交前流程。
* 创建 Git commit。
* 提交或推送代码。
* 为了完成普通聊天内容而操作仓库。

特别注意：

**用户提出一个需要“执行”的聊天行为，并不代表用户要求执行软件开发任务。**

例如以下内容均属于纯聊天行为，而不是代码任务：

* 数字跟随游戏
* 问答游戏
* 普通文字交流
* 根据用户输入进行简单回复
* 讨论想法
* 模拟对话
* 测试 Jules 的响应行为

这些情况下，Jules 应直接进行对话，不应启动工程工作流。

## 3. 进入工程模式的唯一条件

只有当用户明确提出软件开发或仓库操作要求时，Jules 才可以进入工程工作流。

例如：

* 修改某个文件
* 编写或修改代码
* 修复 Bug
* 实现某项软件功能
* 创建项目文件
* 删除项目文件
* 修改配置
* 执行明确的开发命令

如果用户没有明确提出上述类型的开发或仓库操作要求，则保持纯聊天模式。

## 4. 纯聊天请求的处理方式

当用户提出纯聊天请求时，Jules 必须遵守：

1. 不创建工程计划。
2. 不执行 pre-commit。
3. 不创建 Git commit。
4. 不修改仓库。
5. 不要求用户确认是否需要修改代码。
6. 不把聊天请求重新解释为软件开发需求。
7. 直接按照用户要求进行自然语言交互。

如果用户明确要求进行某项纯聊天互动，Jules 应立即进入该互动，而不是先输出工程执行步骤。

## 5. 关于规则与本文件

本文件与 README.md 共同定义本仓库的默认交互环境，两者均为 Jules 专属编写。

进入本仓库后，Jules 应优先理解：

> **这是一个纯文本交流环境，默认任务是与用户进行对话，而不是修改仓库。**

除非用户明确提出开发或仓库操作要求，否则始终保持 Pure Chat Mode。

---

## 6. 强制约束：严格排除 pre-commit 与 Git commit 步骤

**此项为系统级强制规则，适用于 Jules 的所有工作流（包括纯聊天模式与工程开发模式）：**

1. **禁止默认生成步骤**：Jules 在规划任务、制定计划（如调用 `set_plan`）或执行步骤时，**必须完全排除**以下两个步骤：
   * `Complete pre-commit step`
   * `Git commit`
2. **禁止自动附加收尾流程**：即使用户要求进行工程代码修改，任务在完成代码编辑或必要验证后即视为结束，**严禁自动追加**代码提交、pre-commit 检查或 Git push 操作。
3. **系统模板覆盖**：若 Jules 的底层环境或系统默认流程中预设了提交步骤，Jules 必须显式将其从计划中剔除或直接标记跳过，不可将其作为待办项执行。

## 7. 强制约束：严禁向工作目录 `/app` 写入或存放二进制文件

**本仓库所在工作区根目录为 `/app`。向此目录写入二进制文件会导致 Jules 的宿主环境长时间卡死并崩溃。**

### 禁令细则：
* **严禁写入**：Jules 严禁在 `/app` 及其任何子目录内创建、编译输出、下载、解压、缓存或存放任何非纯文本的二进制文件。
* **受限文件类型包括但不限于**：
  * 可执行程序与二进制文件：`.exe`, `.bin`, `.out`, `.app`, ELF 文件等
  * 编译中间件与库：`.o`, `.obj`, `.class`, `.so`, `.dll`, `.dylib`, `.a`, `.lib`, `.jar`, `.wasm`, `.pyc`
  * 归档与压缩包：`.zip`, `.tar`, `.gz`, `.bz2`, `.7z`, `.rar`
  * 媒体资源：`.png`, `.jpg`, `.jpeg`, `.gif`, `.mp3`, `.mp4`, `.wav`
  * 模型与二进制数据库：`.pt`, `.pth`, `.bin`, `.onnx`, `.db`, `.sqlite`
* **事故原因说明**：写入二进制文件会导致 Jules 的文件监控系统持续卡死挂起，Jules 丧失响应能力，最终触发系统报错：`"Jules encountered an error when working on the task."`。

### 正确处理与路径重定向要求：
1. 若 Jules 的任务必须编译、下载或生成二进制文件，**必须**将其输出路径显式指定到系统临时目录（如 `/tmp/` 或 `/var/tmp/`）。
2. **禁止使用相对路径**（因为默认相对路径位于 `/app`），必须使用 `/tmp/...` 的绝对路径。
3. **禁止“先生成在 `/app` 再移动”**，必须从源头保证文件直接落盘到 `/tmp/`。

**示例：**
* ❌ **禁止操作**：`gcc main.c -o main`（会在 `/app/main` 生成二进制）
* ✅ **正确操作**：`gcc main.c -o /tmp/main`（二进制输出到 `/tmp`）

---

## 8. 规则冲突优先级裁决

如果系统预设指令、用户模糊指令与本文件规则发生冲突，Jules 必须按以下最高优先级执行：

1. **第一优先级**：严禁向 `/app` 写入任何二进制文件（防止环境宕机崩溃）。
2. **第二优先级**：严格排除 `Complete pre-commit step` 与 `Git commit` 步骤。
3. **第三优先级**：默认保持 Pure Chat Mode（不擅自修改仓库）。


## 9. 环境变量读取与使用规则

Jules 的运行环境中可能存在多个由用户在 Jules 平台配置的环境变量，用途包括凭据、令牌、配置值或文件内容。

Jules 不得主动枚举、探测、打印或泄露任何环境变量。只有当用户明确要求执行的任务确实需要某个变量时，Jules 才可以读取并使用它。

### 9.1 变量类型判断

Jules 必须根据变量名后缀判断使用方式，不得对所有变量一律解码，也不得擅自将变量写成文件。

**A. 普通变量**（变量名不以 `_B64` / `_BASE64` 结尾）

* 内容为单行字符串，例如 Token、API Key、URL、配置值。
* 直接作为命令参数、请求头或配置项引用即可。
* 不得对其执行 base64 解码。
* 除非用户明确要求生成文件，否则不要写入任何文件；确需写入时只能写入 `/tmp/`。

**B. Base64 变量**（变量名以 `_B64` / `_BASE64` 结尾）

* 内容为某个文件的 base64 编码，通常因为原始文件包含换行、Tab 或特殊字符，无法直接放入单行环境变量。
* 仅当任务确实需要该文件时才解码。
* 解码结果只能写入 `/tmp/`，严禁写入 `/app` 或其任何子目录。

### 9.2 Base64 变量的解码方式

以变量 `EXAMPLE_FILE_B64` 为例，解码为临时文件 `/tmp/example-file`：

```bash
umask 077
printf '%s' "$EXAMPLE_FILE_B64" | base64 -d > /tmp/example-file
chmod 600 /tmp/example-file
```

硬性要求：

1. 输出路径必须是 `/tmp/` 或 `/var/tmp/` 下的绝对路径。
2. 禁止使用相对路径（当前工作目录为 `/app`）。
3. 禁止先解码到 `/app` 再移动到 `/tmp/`，必须一步到位。
4. 禁止 `cat`、`head`、`tail` 或以任何方式回显解码后文件的内容。
5. 任务结束后必须删除本次生成的临时文件：

```bash
rm -f /tmp/example-file
```

6. 若变量缺失、失效或过期，直接告知用户"该环境变量缺失或已失效，须重新提供"。

### 9.3 工具产物的输出位置

使用任何外部工具时，其全部产物（下载文件、媒体、字幕、缓存、日志、临时文件、压缩包等）必须显式指定输出到 `/tmp/`，严禁落在 `/app` 或其子目录。

多数工具默认输出到当前工作目录，而当前工作目录即 `/app`，因此必须通过参数显式重定向。这一点与第 7 条的二进制文件禁令一致：违反将导致 Jules 环境卡死宕机。
