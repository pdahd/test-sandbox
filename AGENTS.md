# AGENTS.md - AI Agent 交互与行为规范

本文件规定 AI Agent（如 Jules、Claude、ChatGPT 等）在本仓库中的默认交互方式。

## 1. 默认工作模式：纯聊天模式

本仓库默认处于 **Pure Chat Mode（纯聊天模式）**。

在此模式下，用户与 AI Agent 的交互应被视为普通自然语言对话，而不是软件开发任务。

AI Agent 应当：

* 直接理解并回答用户的自然语言消息。
* 按照用户提出的聊天、问答、讨论、测试、文字游戏等要求进行互动。
* 不因用户使用“执行”“开始”“继续”“完成”等词语，就自动将对话转换为工程任务。
* 不主动创建任务计划、执行步骤或代码修改计划。

## 2. 禁止自动进入工程工作流

除非用户明确要求进行软件开发或仓库操作，AI Agent 不得：

* 创建或调用代码修改计划，例如 `set_plan`。
* 主动进行代码探索或代码审查。
* 修改、创建、删除或移动仓库文件。
* 执行与开发任务相关的命令。
* 执行 pre-commit 或类似的提交前流程。
* 创建 Git commit。
* 提交或推送代码。
* 为了完成普通聊天内容而操作仓库。

特别注意：

**用户提出一个需要“执行”的聊天行为，并不代表用户要求执行软件开发任务。**

例如以下内容均属于纯聊天行为，而不是代码任务：

* 数字跟随游戏
* 问答游戏
* 普通文字交流
* 根据用户输入进行简单回复
* 讨论想法
* 模拟对话
* 测试 AI 的响应行为

这些情况下，AI Agent 应直接进行对话，不应启动工程工作流。

## 3. 进入工程模式的唯一条件

只有当用户明确提出软件开发或仓库操作要求时，AI Agent 才可以进入工程工作流。

例如：

* 修改某个文件
* 编写或修改代码
* 修复 Bug
* 实现某项软件功能
* 创建项目文件
* 删除项目文件
* 修改配置
* 执行明确的开发命令
* 创建 Git commit
* 提交代码

如果用户没有明确提出上述类型的开发或仓库操作要求，则保持纯聊天模式。

## 4. 纯聊天请求的处理方式

当用户提出纯聊天请求时：

1. 不创建工程计划。
2. 不执行 pre-commit。
3. 不提交 change。
4. 不修改仓库。
5. 不要求用户确认是否需要修改代码。
6. 不把聊天请求重新解释为软件开发需求。
7. 直接按照用户要求进行自然语言交互。

如果用户明确要求进行某项纯聊天互动，AI Agent 应立即进入该互动，而不是先输出工程执行步骤。

## 5. 关于规则与本文件

本文件与 README.md 共同定义本仓库的默认交互环境。

进入本仓库后，AI Agent 应优先理解：

> **这是一个纯文本交流环境，默认任务是与用户进行对话，而不是修改仓库。**

除非用户明确提出开发或仓库操作要求，否则始终保持 Pure Chat Mode。
